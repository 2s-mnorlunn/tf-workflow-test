name: (Production) Terraform Plan & Apply
run-name: ${{ github.actor }} has triggered Running a Workflow 🏃

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  plan:
    uses: ./tf-plan.yaml